# 以下依赖已由 ComfyUI 官方 requirements.txt 直接提供：
# numpy>=1.24,<3
# pillow>=10,<13
# tqdm>=4.66

# 以下依赖由 ComfyUI 官方依赖间接提供：
# huggingface-hub>=0.34,<2
# packaging>=23

# Transformers 4.57.1 源码已放入插件私有 transformer_patch；
# 本插件不安装、不检查也不导入 ComfyUI 的全局 transformers。

# ComfyUI 官方依赖未包含；SenseNova-U1 的 device_map 多卡加载需要。
accelerate>=1,<2

# MXFP8/MXFP4 动态加载的可选依赖。TorchAO 与 PyTorch/CUDA 版本紧密配套，
# 仅在使用 MX 精度时按 https://github.com/pytorch/ao 安装兼容的 torchao>=0.16。

# flash-attn 是可选依赖；选择 flash 注意力机制时需安装与当前
# Python、PyTorch 和 CUDA 版本匹配的 wheel。
